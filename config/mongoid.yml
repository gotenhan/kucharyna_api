development:
  sessions:
    default:
      database: kucharyna_api_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: kucharyna_api_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0
